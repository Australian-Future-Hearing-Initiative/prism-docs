
The PRISM web frontend serves as the interface for data collection and study management for **Researchers** conducting "experiments" within the **PRISM** ecosystem as part of the [Australian Future Hearing Initiative](https://github.com/Australian-Future-Hearing-Initiative).

---

### For Researchers
Researchers use this application to design experiments, manage study cohorts, and deploy tasks to participants. It is the control center for your data collection needs.

**How to use:**
1.  **Login**: Access the researcher dashboard securely.
2.  **Manage Projects**: Create new projects to house specific studies or experiments.
3.  **Configure Experiments**:
    *   Set up various experiment types (e.g., Ecological Momentary Assessment (EMA) surveys, sensor-thresholds).
    *   Define parameters and logical flows for the experiments.
4.  **Manage Participants**:
    *   Add new participants to the system.
    *   Assign participants to specific projects/cohorts.
    *   Monitor participant progress through assigned tasks.
    *   Email participants.
    *   Download audiograms

See the complete userguide: https://docs.google.com/document/d/1_FZVBiN1QqhP50A2izS7KlW_Y021koJM45pSYcHEkMw/edit?tab=t.0#heading=h.qbhdu5cvo4bq
---

### Developer Instructions

#### 1. Launch the Backend Server
This frontend relies on the [prism-flutter-backend](https://github.com/Australian-Future-Hearing-Initiative/prism-flutter-backend/blob/main/README.md). See the repo for launch instructions.

#### 2. Configure Environment
Create a `.env` file in the **root directory** and add the URL of the backedn server, e.g.:

```env
## Base URL for API calls when running locally
API_BASE_URL=http://127.0.0.1:8000
```

#### 3. Install FVM (Flutter Version Management)
We use FVM to ensure consistent Flutter versions.

*   Option 1 **Dart Pub**: `dart pub global activate fvm`
*   Option 2 **Homebrew**: `brew tap leoafarias/fvm && brew install fvm`
*   Option 3 **Install Script**: `curl -fsSL https://fvm.app/install.sh | bash`

#### 4. Run the App
Execute the following commands from the root of this repository:

```bash
fvm flutter channel stable
fvm flutter upgrade
fvm flutter pub get
## fvm dart run build_runner watch -d    # Optional: watch freezed/json files for changes
fvm flutter run -d chrome             # Run the app in Chrome
```

#### 🧪 Testing & Architecture

*   **Unit & Widget Tests**: `flutter test`
*   **Specific Test**: `flutter test <path/to/test.dart>`
*   **Integration Tests**:
    ```bash
    flutter drive --driver=test_driver/integration_test.dart --target=integration_test/participant_flow_test.dart -d chrome
    ```
    *Note: Web integration tests inject bytes directly as WebDriver screens cannot handle drag-and-drop.*
*   **Coverage**: `flutter test --coverage`

**Key Directory Structure:**
*   `lib/ui`: Contains all screens and widgets, separated by feature (e.g., `experiment`, `participant`, `auth`).
*   `lib/domain`: Business logic and models.
*   `lib/data`: Data repositories and API services.

#### TODO: Deployment Instructions
